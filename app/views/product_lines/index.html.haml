- title "Product Lines"

%p Here are the produt lines currently in the system.

- @product_lines.group_by(&:manufacturer).each do |manufacturer,product_lines|

  %h2= manufacturer.try(:name)

  %table.interactive_table
    %thead
      %tr
        %th Prod no
        %th Name
        %th Price
        %th{:colspan => '3'} Actions
    %tbody
      - for product_line in product_lines
        %tr
          %td= h product_line.prod_no
          %td= h product_line.name
          %td=  number_to_currency(product_line.price, :unit => '&pound;')
          %td= link_to 'Show', product_line
          %td= link_to 'Edit', edit_product_line_path(product_line)
          %td= link_to 'Destroy', product_line, :confirm => 'Are you sure?', :method => :delete

%p
  %strong Choices:
  = link_to "New Product Line", new_product_line_path

