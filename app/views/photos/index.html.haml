- title "Photos"

%p Here are the product line photos that have been added to the system.

%table.interactive_table
  %thead
    %tr
      %th Product Line
      %th Title
      %th Position
      %th Image
  %tbody
    - for photo in @photos
      %tr
        %td= h photo.product_line.try(:prod_no)
        %td= h photo.title
        %td= h photo.position
        %td= image_tag thumb_photo_path(photo,:format => :jpg)

        %td= link_to 'Show', photo_path(photo)
        %td= link_to 'Edit', edit_photo_path(photo)
        %td= link_to 'Destroy', photo, :confirm => 'Are you sure?', :method => :delete

%p
  %strong Choices:
  = link_to "New Photo", new_photo_path

